if (screen.width >= 900) 
var space = $(window).height();
var diff = space - HEIGHT;
var margin = (diff > 0) ? (space - HEIGHT)/2 : 0;
$('#video').css({'height': espace});
alert("tamaño de espace: "+ espace);
